{
  "name": "baukalk-pro",
  "version": "0.1.0",
  "private": true,
  "description": "BauKalk Pro — Baukalkulations-Software (kalku.de)",
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "test": "pnpm -r test",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
